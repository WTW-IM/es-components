{
  "name": "es-components-monorepo",
  "private": true,
  "license": "MIT",
  "scripts": {
    "ci-run": "sh ci.sh",
    "version_only": "lerna publish --skip-npm --force-publish",
    "publish_only": "lerna publish --skip-git",
    "publish": "lerna publish",
    "link": "lerna link",
    "start": "npm start --prefix packages/es-components",
    "commit": "commit",
    "preprepare": "npm run build -w es-components-shared-types -w es-components-via-theme -w es-components-wtw-theme",
    "prepare": "husky install",
    "test": "npm test --workspaces --if-present"
  },
  "workspaces": [
    "shared/*",
    "packages/*"
  ],
  "lint-staged": {
    "packages/es-components/src/**/*.{js,jsx,ts,tsx}": [
      "prettier --single-quote --write",
      "npm exec --prefix packages/es-components eslint --fix"
    ]
  },
  "devDependencies": {
    "@aabenoja/conventional-changelog-eslint-lerna": "^1.0.2",
    "@commitlint/cli": "^17.3.0",
    "@commitlint/config-conventional": "^17.3.0",
    "@commitlint/prompt-cli": "^17.3.0",
    "@rollup/plugin-babel": "^6.0.3",
    "@types/node": "^18.11.17",
    "commitlint": "^17.3.0",
    "conventional-changelog": "^3.1.25",
    "husky": "^8.0.2",
    "lerna": "^6.1.0",
    "lint-staged": "^13.1.0",
    "prettier": "^2.8.1"
  }
}
